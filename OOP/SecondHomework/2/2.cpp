
#include <iostream>
#include <fstream>
#include "SectionVotes.hpp"
#include "ElectionResultsDatabase.hpp"
#include <string>
using namespace std;

int main()
{
	SectionVotes sec(1, 2, 3);
	
	/*ElectionResultsDatabase data;
	ifstream infile("example.txt");
	if (infile.is_open()) {
		while (infile.good()) {
			infile >> data;
		}
		
	}
	
	infile.close();
	ofstream outfile("test.txt");
	if (outfile.is_open()) {
		outfile << data;
	}

	outfile.close();
	cout << data.votesForParty(Party::PARTY1) << " " << data.votesForParty(Party::PARTY2) << " " << data.votesForParty(Party::PARTY3) << endl;

	cout << data;*/

	
}


